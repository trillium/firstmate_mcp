# Durable feature manifest for firstmate_mcp (beads epic task-8d0ah, ARCHITECTURE).
#
# Every tracked capability carries a stable identity, upstream provenance,
# behavioral contract pointer, upstream test provenance, Python and TypeScript
# implementation status, and explicit divergence status/reason where our
# behavior intentionally differs. Local-only Trillium extensions live in the
# same manifest with kind `local` so they survive upstream reconciliation.
#
# Authority rules:
#   - Script headers own behavior; schema/contracts.yaml owns the depended-on
#     declaration; drift/baseline.json owns the observed-surface pin.
#   - This file tracks capability identity and status only. It never
#     re-declares output schemas, flag subsets, or envelope constants — it
#     points at the files that do.
#   - A status must never claim an implementation that does not exist:
#     `manifest/validate.py` spot-checks every `implemented` evidence pointer
#     against the tree.
#
# Validate: python3 manifest/validate.py
# Human view: manifest/README.md
version: v1
upstream:
  repo: https://github.com/kunchenguid/firstmate.git
  submodule: sources/firstmate
  gitlink_at_seed: 1b1b6e051dafc9dcabe3ef0a7d4a64bd40a45567
  baseline: drift/baseline.json
  baseline_rev_at_seed: aaf67489
  baseline_surfaces_at_seed: 163
  role: radar
  note: >-
    Upstream fingerprint for early-warning radar only: the kunchenguid pin
    (submodule gitlink) plus the drift baseline revision/surface count.
    drift/shift.py watches this pin against upstream main; a shift report
    is a deliberate port decision, never an automatic merge. Nothing runs
    against this copy — see `fork` below for the working line.
fork:
  repo: https://github.com/trillium/firstmate.git
  proven_commit: 86c035336bf9e136dd44d76e7d46e16d260fac8f
  proven_date: "2026-09-17"
  proven_subject: "feat(wake): fold inbox-supervision delivery into one PR (task-z8sn4) (#160)"
  merge_base_with_upstream: 53ecbc7961ac066461ea3104c713d81d40fb32d6
  merge_base_date: 2026-08-10
  commits_ahead_at_seed: 204
  basis: UPSTREAM-RESULTS.md seed 2026-09-18 plus CUTOVER-PROOF.md basis 2026-09-17
  role: working-copy
  note: >-
    Working-copy pin: the trillium/firstmate line the adapter was proven
    against (fork HEAD at both proofs, ~204 commits ahead of the Aug-10
    merge base with upstream, carrying the beads-substrate migration).
    Everything runs against this line; re-pin here whenever the adapter is
    re-proven against a newer fork commit.
features:
  # ---- SUPPORTED: behavior-identical reads + safe steer (no approval) ----
  - id: fleet_snapshot
    kind: upstream-mirror
    summary: Canonical whole-fleet read; typed projection equals the owning script's observable output modulo the envelope wrap.
    upstream_command: bin/fm-fleet-snapshot.sh
    upstream_flags: ["--json"]
    contract: schema/contracts.yaml#fleet_snapshot
    upstream_test: "ts/tests/conformance.test.ts::test_fleet_snapshot_matches_direct (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:fleet_snapshot"]}
    divergence: {status: none, reason: ""}
  - id: backlog
    kind: upstream-mirror
    summary: Backlog records plus per-state counts derived from the fleet snapshot instead of reimplementing a backend.
    upstream_command: "derived: fleet_snapshot (bin/fm-fleet-snapshot.sh --json)"
    upstream_flags: ["--json"]
    contract: schema/contracts.yaml#backlog
    upstream_test: "ts/tests/conformance.test.ts::test_backlog_derives_counts_from_same_snapshot"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:backlog"]}
    divergence: {status: none, reason: ""}
  - id: crew_state
    kind: upstream-mirror
    summary: Deterministic current-state read; never infers state from the status log tail.
    upstream_command: bin/fm-crew-state.sh
    upstream_flags: ["<id>"]
    contract: schema/contracts.yaml#crew_state
    upstream_test: "ts/tests/conformance.test.ts::test_crew_state_unknown_matches_direct_line"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:crew_state"]}
    divergence: {status: none, reason: ""}
  - id: status_tail
    kind: upstream-mirror
    summary: Wake-event history tail for one task; history only, never current state.
    upstream_command: "file: state/<id>.status"
    upstream_flags: ["<id>", "lines=1..50"]
    contract: schema/contracts.yaml#status_tail
    upstream_test: "ts/tests/conformance.test.ts::test_status_tail_matches_file_tail (plus traversal-refusal proof)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:status_tail"]}
    divergence:
      status: intentional
      reason: >-
        Bounded 50-line cap with id validation and path confinement; the
        upstream state file itself is unbounded. History only by contract —
        never presented as current state.
  - id: send_message
    kind: upstream-mirror
    summary: Safest exposed write; steers one crew with a verified literal prose line.
    upstream_command: bin/fm-send.sh
    upstream_flags: ["<target>", "<text>"]
    contract: schema/contracts.yaml#send_message
    upstream_test: "ts/tests/server.test.ts (stub-home server proof; writes never dispatch in conformance by design — ts/tests/conformance.test.ts::test_write_tools_never_dispatch)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:send_message"]}
    divergence:
      status: intentional
      reason: >-
        500-char single-line cap with slash-command refusal on the plain-text
        path only; upstream fm-send.sh accepts broader input and --key/--raw
        lifecycle verbs, which stay out of scope per the smarts-only line.
  - id: fleet_poll
    kind: upstream-mirror
    summary: Polling convenience over the fleet snapshot; the snapshot stays canonical.
    upstream_command: bin/fm-fleet-snapshot.sh
    upstream_flags: ["--json"]
    contract: ts/src/tools.ts#fleet_poll (poll projection over the fleet_snapshot contract)
    upstream_test: "ts/tests/conformance.test.ts::test_fleet_poll_agrees_with_snapshot"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:fleet_poll"]}
    divergence:
      status: intentional
      reason: >-
        Adapter-native poll projection; upstream ships no poll surface. Derived
        from fleet_snapshot output on every call, and fleet_snapshot stays the
        canonical read.
  - id: peek
    kind: upstream-mirror
    summary: Bounded endpoint tail for cheap diagnosis; target plus line count echoed.
    upstream_command: bin/fm-peek.sh
    upstream_flags: ["<target>", "lines=1..100"]
    contract: schema/contracts.yaml#peek
    upstream_test: "ts/tests/conformance.test.ts::TestPeekEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:peek"]}
    divergence:
      status: intentional
      reason: >-
        Bounded 100-line cap with id validation; the upstream tail arg is
        unbounded. Envelope truncates long tails at 8KB.
  - id: fleet_view
    kind: upstream-mirror
    summary: Human render of the fleet snapshot for operators; the snapshot stays canonical.
    upstream_command: bin/fm-fleet-view.sh
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#fleet_view
    upstream_test: "ts/tests/conformance.test.ts::TestFleetViewEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:fleet_view"]}
    divergence:
      status: intentional
      reason: >-
        Human render only with no flag subset; --json prints the underlying
        snapshot which fleet_snapshot already serves. Envelope truncates at 8KB.
  - id: review_diff
    kind: upstream-mirror
    summary: Branch-vs-base review diff for one task worktree; stat flag echoes.
    upstream_command: bin/fm-review-diff.sh
    upstream_flags: ["<task-id>", "--stat"]
    contract: schema/contracts.yaml#review_diff
    upstream_test: "ts/tests/conformance.test.ts::TestReviewDiffEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:review_diff"]}
    divergence:
      status: intentional
      reason: >-
        Closed --stat flag subset with id validation; envelope truncates
        long diffs at 8KB.
  - id: bearings_snapshot
    kind: upstream-mirror
    summary: Compact pick-up digest projected from the fleet snapshot, local-only.
    upstream_command: bin/fm-bearings-snapshot.sh
    upstream_flags: ["--json"]
    contract: schema/contracts.yaml#bearings_snapshot
    upstream_test: "ts/tests/conformance.test.ts::TestBearingsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:bearings_snapshot"]}
    divergence:
      status: intentional
      reason: >-
        Local-only --json projection; the --include-prs network path stays
        out of scope. Schema fm-bearings.v1 validated before return.
  - id: wake_drain
    kind: upstream-mirror
    summary: Drained-wake records from the durable watcher queue; the drain is the read.
    upstream_command: bin/fm-wake-drain.sh
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#wake_drain
    upstream_test: "ts/tests/conformance.test.ts::TestWakeDrainEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:wake_drain"]}
    divergence:
      status: intentional
      reason: >-
        No flag subset; the queue drain is the defined read. Envelope
        truncates at 8KB.
  - id: guard_check
    kind: upstream-mirror
    summary: Watcher liveness and worktree-tangle verdict read; warn-only, never blocks.
    upstream_command: bin/fm-guard.sh
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#guard_check
    upstream_test: "ts/tests/conformance.test.ts::TestGuardCheckEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:guard_check"]}
    divergence:
      status: intentional
      reason: >-
        Served with FM_GUARD_READ_ONLY=1 so the mirror never clears banner
        state; upstream defaults clear. Always exits 0 by contract.
  - id: remote_doctor
    kind: upstream-mirror
    summary: Remote-home readiness diagnostic read; check mode only, repairs stay out.
    upstream_command: bin/fm-remote-doctor.sh
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#remote_doctor
    upstream_test: "ts/tests/conformance.test.ts::TestRemoteDoctorEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:remote_doctor"]}
    divergence:
      status: intentional
      reason: >-
        Check mode only with no flag subset; the --fix repair path stays
        out of scope. Envelope truncates at 8KB.
  - id: remote_file
    kind: upstream-mirror
    summary: Bounded read of one home-relative file; get only, intake stays out.
    upstream_command: bin/fm-remote-file.sh
    upstream_flags: ["get", "<relative-path>", "<max-bytes>"]
    contract: schema/contracts.yaml#remote_file
    upstream_test: "ts/tests/conformance.test.ts::TestRemoteFileEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:remote_file"]}
    divergence:
      status: intentional
      reason: >-
        Get-only subset with adapter-side traversal refusal and a bounded
        8KB default byte cap; upstream defaults to 256KB and owns the put
        handoff-intake path, which stays out. Envelope truncates at 8KB.
  - id: remote_delta
    kind: upstream-mirror
    summary: Continuity-checked delta read of one append-only log; never consumes the source.
    upstream_command: bin/fm-remote-delta-read.sh
    upstream_flags: ["<relative-log>", "<offset>", "<prefix-sha256>"]
    contract: schema/contracts.yaml#remote_delta
    upstream_test: "ts/tests/conformance.test.ts::TestRemoteDeltaEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:remote_delta"]}
    divergence:
      status: intentional
      reason: >-
        The wait window is clamped to 0..10s so no call nears the 30s
        envelope; upstream allows 300s. Adapter-side cursor and hash-shape
        validation refuses before spawning.
  - id: handoff_status
    kind: upstream-mirror
    summary: Staged handoff outbox read; list staged moves or read one outbox tail.
    upstream_command: "file: data/handoff/<id>.outbox.md"
    upstream_flags: ["<id>", "lines=1..20"]
    contract: schema/contracts.yaml#handoff_status
    upstream_test: "ts/tests/conformance.test.ts::TestHandoffStatusEquivalence (adapter-vs-file equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:handoff_status"]}
    divergence:
      status: intentional
      reason: >-
        Bounded native read with id validation and path confinement; no
        owning-script status verb exists. Staged moves only — handoff owns
        moves, receive owns intake.
  - id: harness_detect
    kind: upstream-mirror
    summary: Harness detection for this home; closed mode subset, never process ancestry.
    upstream_command: bin/fm-harness.sh
    upstream_flags: ["own", "crew", "secondmate", "secondmate-model", "secondmate-effort"]
    contract: schema/contracts.yaml#harness_detect
    upstream_test: "ts/tests/conformance.test.ts::TestHarnessEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:harness_detect"]}
    divergence:
      status: intentional
      reason: >-
        Closed detection-mode subset with a default of own; ancestry and
        ancestry-descent pid walks plus validate-native-effort stay out.
        Envelope truncates at 8KB.
  - id: project_mode
    kind: upstream-mirror
    summary: Registered delivery posture (mode + yolo) for one project.
    upstream_command: bin/fm-project-mode.sh
    upstream_flags: ["<project>"]
    contract: schema/contracts.yaml#project_mode
    upstream_test: "ts/tests/conformance.test.ts::TestProjectModeEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:project_mode"]}
    divergence:
      status: intentional
      reason: >-
        Mapped mode+yolo projection with project validation; --raw stays
        out (denied argv flag), so conditional policies resolve to their
        most rigorous leg exactly as upstream maps them.
  - id: lock_status
    kind: upstream-mirror
    summary: Per-home session lock status; acquiring stays out.
    upstream_command: bin/fm-lock.sh
    upstream_flags: ["status"]
    contract: schema/contracts.yaml#lock_status
    upstream_test: "ts/tests/conformance.test.ts::TestLockStatusEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lock_status"]}
    divergence:
      status: intentional
      reason: >-
        Status subcommand only with a free/held/stale/unreadable
        projection; acquiring would steal firstmate's own session lock and
        stays out. Always exits 0 by contract.
  - id: lease_check
    kind: upstream-mirror
    summary: Per-task supervision lease check; claim/release/sweep stay out.
    upstream_command: bin/fm-lease.sh
    upstream_flags: ["check", "<task>"]
    contract: schema/contracts.yaml#lease_check
    upstream_test: "ts/tests/conformance.test.ts::TestLeaseCheckEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lease_check"]}
    divergence:
      status: intentional
      reason: >-
        Check subcommand only with id validation; the defined unleased
        outcome (exit 1, empty) projects to leased:false. Claim, release,
        release-actor, and sweep are guarded by the supervision-actor
        contract and stay out.
  - id: bearings_board_path
    kind: upstream-mirror
    summary: Stable path of the captain's bearings board; building/arming stays out.
    upstream_command: bin/fm-bearings-board.sh
    upstream_flags: ["path"]
    contract: schema/contracts.yaml#bearings_board_path
    upstream_test: "ts/tests/conformance.test.ts::TestBearingsBoardEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:bearings_board_path"]}
    divergence:
      status: intentional
      reason: >-
        Path subcommand only; build validates a payload, proves a live
        lavish session, and binds the keyed-answer intake — interactive
        captain surface that stays firstmate/agent-owned.
  - id: inbox_status
    kind: upstream-mirror
    summary: Captain inbox status from durable records; sends no wake.
    upstream_command: bin/fm-inbox.sh
    upstream_flags: ["status"]
    contract: schema/contracts.yaml#inbox_status
    upstream_test: "ts/tests/conformance.test.ts::TestInboxEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:inbox_status"]}
    divergence:
      status: intentional
      reason: >-
        Status subcommand only — durable-records read with no network and
        no wake appended. note/say queue wakes or call Bedrock and drain
        --ack moves records, so all stay out. Envelope truncates at 8KB.
  - id: inbox_list
    kind: upstream-mirror
    summary: Queued captain inbox notes; list only.
    upstream_command: bin/fm-inbox.sh
    upstream_flags: ["list"]
    contract: schema/contracts.yaml#inbox_list
    upstream_test: "ts/tests/conformance.test.ts::TestInboxEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:inbox_list"]}
    divergence:
      status: intentional
      reason: >-
        List subcommand only; same out-of-scope set as inbox_status.
        Envelope truncates at 8KB.
  - id: home_summary
    kind: upstream-mirror
    summary: Published home-summary ledger; the refresh stays firstmate-owned.
    upstream_command: "file: state/home-summary.json"
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#home_summary
    upstream_test: "ts/tests/conformance.test.ts::TestHomeSummaryEquivalence (adapter-vs-ledger equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:home_summary"]}
    divergence:
      status: intentional
      reason: >-
        Bounded ledger read with path confinement and schema
        fm-secondmate-home-summary.v1 validation; the refresh itself is
        firstmate-owned plumbing (session start, watcher, spawn, teardown
        call it --best-effort).
  - id: contributions_snapshot
    kind: upstream-mirror
    summary: Owned-contribution coverage projected from the fleet snapshot; never contacts a forge.
    upstream_command: bin/fm-contributions.sh
    upstream_flags: ["snapshot", "<contribution-input>", "--all"]
    contract: schema/contracts.yaml#contributions_snapshot
    upstream_test: "ts/tests/conformance.test.ts::TestContributionsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:contributions_snapshot"]}
    divergence:
      status: intentional
      reason: >-
        Snapshot only over the same contribution-input the fleet snapshot
        stages, with a closed --all flag; poll spends forge reads over the
        network and verdict/ack/arm mutate the saved records, so all stay
        out. Envelope truncates at 8KB.
  - id: contributions_pending
    kind: upstream-mirror
    summary: Pending contribution event tokens from saved records.
    upstream_command: bin/fm-contributions.sh
    upstream_flags: ["pending"]
    contract: schema/contracts.yaml#contributions_pending
    upstream_test: "ts/tests/conformance.test.ts::TestContributionsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:contributions_pending"]}
    divergence:
      status: intentional
      reason: >-
        Pending subcommand only; same out-of-scope set as
        contributions_snapshot.
  - id: mail_status
    kind: upstream-mirror
    summary: Mail configuration and last poll cursor; no network, no wake.
    upstream_command: bin/fm-mail.sh
    upstream_flags: ["status"]
    contract: schema/contracts.yaml#mail_status
    upstream_test: "ts/tests/conformance.test.ts::TestMailEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:mail_status"]}
    divergence:
      status: intentional
      reason: >-
        Status subcommand only — config plus cursor with no network and
        no wake. read needs IMAP credentials from the home .env (outside
        MCP); poll queues wakes and its check-arm stays firstmate-owned.
  - id: mail_read
    kind: upstream-mirror
    summary: Unseen-INBOX digest over BODY.PEEK; mail stays unseen until firstmate answers.
    upstream_command: bin/fm-mail.sh
    upstream_flags: ["read"]
    contract: schema/contracts.yaml#mail_read
    upstream_test: "ts/tests/conformance.test.ts::TestMailEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:mail_read"]}
    divergence:
      status: intentional
      reason: >-
        Read subcommand only with a never-marks-seen warning echoed;
        BODY.PEEK is owned by the script. Credentials live outside MCP
        in the home .env and never cross args or logs; inert without them.
  - id: voice_status
    kind: upstream-mirror
    summary: Voice-agent status answer from durable records; no mic, no Bedrock, no audio.
    upstream_command: bin/fm_voice_records.py
    upstream_flags: ["status", "--scope"]
    contract: schema/contracts.yaml#voice_status
    upstream_test: "ts/tests/conformance.test.ts::TestVoiceEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:voice_status"]}
    divergence:
      status: intentional
      reason: >-
        Status subcommand only with a closed counts/full scope (default
        counts, safe by construction); full only via the captain's own
        read-scope with the helper's deny list enforced inside. The mic
        client and Bedrock relay have no tool — mic hardware and AWS
        credentials stay out of MCP.
  - id: lint_versions
    kind: upstream-mirror
    summary: Required ShellCheck/actionlint pins from the lint owners; probes only.
    upstream_command: bin/fm-lint.sh
    upstream_flags: ["--required-version"]
    contract: schema/contracts.yaml#lint_versions
    upstream_test: "ts/tests/conformance.test.ts::TestInstallsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lint_versions"]}
    divergence:
      status: intentional
      reason: >-
        Composite read of both required-version probes (fm-lint.sh plus
        fm-lint-workflows.sh); the lint runs and all installers stay out.
  - id: tool_update_check
    kind: upstream-mirror
    summary: Watched-tool update report; repairs nothing, installs nothing.
    upstream_command: bin/fm-tool-update-check.sh
    upstream_flags: ["check"]
    contract: schema/contracts.yaml#tool_update_check
    upstream_test: "ts/tests/conformance.test.ts::TestInstallsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:tool_update_check"]}
    divergence:
      status: intentional
      reason: >-
        Check subcommand only; arm/disarm mutate the watcher and stay
        out. The sweep reports and never installs, updates, or reorders PATH.
  - id: vendor_auth_probe
    kind: upstream-mirror
    summary: Bounded vendor auth probe; raw output is classified, never printed.
    upstream_command: bin/fm-vendor-auth-probe.sh
    upstream_flags: ["<probe>"]
    contract: schema/contracts.yaml#vendor_auth_probe
    upstream_test: "ts/tests/conformance.test.ts::TestInstallsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:vendor_auth_probe"]}
    divergence:
      status: intentional
      reason: >-
        Closed single-probe allowlist with the script's hard-bounded
        non-interactive argv; exit never encodes the result. Raw vendor
        output is classified inside and never leaves the script.
  - id: startup_memory
    kind: upstream-mirror
    summary: Startup-memory budget read or local estimate; never creates config.
    upstream_command: bin/fm-startup-memory-budget.sh
    upstream_flags: ["read", "report"]
    contract: schema/contracts.yaml#startup_memory
    upstream_test: "ts/tests/conformance.test.ts::TestInstallsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:startup_memory"]}
    divergence:
      status: intentional
      reason: >-
        Read/report modes only; absent or invalid config is a typed
        error, never an inferred default. Bootstrap owns materialization.
  - id: pr_state
    kind: upstream-mirror
    summary: Visible blockers on one GitHub pull request; never posts or merges.
    upstream_command: bin/fm-pr-state.sh
    upstream_flags: ["<pr-url>"]
    contract: schema/contracts.yaml#pr_state
    upstream_test: "ts/tests/conformance.test.ts::TestSmallGapsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:pr_state"]}
    divergence:
      status: intentional
      reason: >-
        GitHub PR URLs only (adapter refuses before spawning what the
        script refuses after); blockers never change the exit. Posting,
        requesting, approving, and merging stay out.
  - id: relay_poll
    kind: upstream-mirror
    summary: Short bounded poll of the relay connector; hard no-op without consent.
    upstream_command: bin/fm-x-poll.sh
    upstream_flags: ["<none>"]
    contract: schema/contracts.yaml#relay_poll
    upstream_test: "ts/tests/conformance.test.ts::TestSmallGapsEquivalence (adapter-vs-script equivalence under a scratch FM_HOME)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:relay_poll"]}
    divergence:
      status: intentional
      reason: >-
        No-arg poll only; inert without the FMX pairing token, which
        lives outside MCP. Failures mean no wake this cycle, never a merge.
  # ---- CHANGED: same owning scripts, narrower surface, per-call approval ----
  - id: lifecycle_interrupt
    kind: upstream-mirror
    summary: Deliver the harness interrupt sequence to one crew; the agent keeps running.
    upstream_command: bin/fm-control.sh
    upstream_flags: ["<id>", "interrupt"]
    contract: ts/src/tools.ts#lifecycle_interrupt (safe verb subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lifecycle_interrupt"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed interrupt verb;
        upstream fm-control.sh knows no MCP approval string. Same owning
        script, narrower surface.
  - id: lifecycle_exit
    kind: upstream-mirror
    summary: Stop one agent, preserving its endpoint, worktree, and uncommitted changes.
    upstream_command: bin/fm-control.sh
    upstream_flags: ["<id>", "exit"]
    contract: ts/src/tools.ts#lifecycle_exit (safe verb subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lifecycle_exit"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed exit verb;
        upstream fm-control.sh knows no MCP approval string. Same owning
        script, narrower surface.
  - id: lifecycle_relaunch
    kind: upstream-mirror
    summary: Transactionally replace one running agent in the same endpoint and worktree.
    upstream_command: bin/fm-control.sh
    upstream_flags: ["<id>", "relaunch"]
    contract: ts/src/tools.ts#lifecycle_relaunch (safe verb subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lifecycle_relaunch"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed relaunch verb with
        progress note; upstream fm-control.sh knows no MCP approval string.
        Same owning script, narrower surface.
  - id: lifecycle_suspend
    kind: upstream-mirror
    summary: Park one persistent secondmate with a durable note.
    upstream_command: bin/fm-control.sh
    upstream_flags: ["<id>", "suspend"]
    contract: ts/src/tools.ts#lifecycle_suspend (safe verb subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lifecycle_suspend"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed suspend verb with
        park note; upstream fm-control.sh knows no MCP approval string. Same
        owning script, narrower surface.
  - id: lifecycle_resume
    kind: upstream-mirror
    summary: Resume one parked persistent secondmate.
    upstream_command: bin/fm-control.sh
    upstream_flags: ["<id>", "resume"]
    contract: ts/src/tools.ts#lifecycle_resume (safe verb subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:lifecycle_resume"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed resume verb;
        upstream fm-control.sh knows no MCP approval string. Same owning
        script, narrower surface.
  - id: spawn_crew
    kind: upstream-mirror
    summary: Spawn one direct report under an explicit delivery contract.
    upstream_command: bin/fm-spawn.sh
    upstream_flags: ["<task-id>", "<project>", "--mode", "--yolo"]
    contract: schema/contracts.yaml#spawn_crew
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:spawn_crew"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the safe flag subset only
        (no repo-mutation options); upstream fm-spawn.sh knows no MCP approval
        string. Dispatch write with no repo-mutation surface.
  - id: scaffold_brief
    kind: upstream-mirror
    summary: Scaffold one crewmate brief; launches nothing.
    upstream_command: bin/fm-brief.sh
    upstream_flags: ["<task-id>", "<repo>", "--mode/--scout"]
    contract: schema/contracts.yaml#scaffold_brief
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:scaffold_brief"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the safe flag subset only;
        upstream fm-brief.sh knows no MCP approval string. Scaffolds
        instructions without launching.
  - id: decision_hold
    kind: upstream-mirror
    summary: Record a durable captain-held decision with a stable hold identity.
    upstream_command: bin/fm-decision-hold.sh
    upstream_flags: ["hold", "<origin-id>", "<decision-key>"]
    contract: ts/src/tools.ts#decision_hold (safe subcommand subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:decision_hold"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the safe hold-subcommand
        subset; upstream fm-decision-hold.sh knows no MCP approval string.
        Same owning script, narrower surface.
  - id: decision_resolve
    kind: upstream-mirror
    summary: Resolve a captain-held decision after a durable decision record is linked.
    upstream_command: bin/fm-decision-hold.sh
    upstream_flags: ["resolve", "<origin-id>", "<decision-key>"]
    contract: ts/src/tools.ts#decision_resolve (safe subcommand subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:decision_resolve"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the safe resolve-subcommand
        subset; upstream fm-decision-hold.sh knows no MCP approval string.
        Same owning script, narrower surface.
  - id: review_decision
    kind: upstream-mirror
    summary: Record a captain approve, decline, or comment on a captain-held task.
    upstream_command: bin/fm-captain-hold.sh
    upstream_flags: ["answer", "<id>", "--decision-file"]
    contract: ts/src/tools.ts#review_decision (safe verdict subset + approval gate)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:review_decision"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed
        approve/decline/comment verdict set recorded as the captain's words
        through the answer subcommand only (never --release); upstream
        fm-captain-hold.sh knows no MCP approval string and no verdict set.
        Ported off bin/fm-review-decision.sh, removed upstream since the pin.
  - id: relay_reply
    kind: upstream-mirror
    summary: Send one public reply; inert without relay consent, which lives outside MCP.
    upstream_command: bin/fm-x-reply.sh
    upstream_flags: ["<target>", "<text>"]
    contract: ts/src/tools.ts#relay_reply (approval + external relay consent)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:relay_reply"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate on top of external relay consent,
        which is never duplicated inside MCP; upstream fm-x-reply.sh knows no
        MCP approval string. Inert without that consent.
  - id: relay_dismiss
    kind: upstream-mirror
    summary: Dismiss one public item; inert without relay consent, which lives outside MCP.
    upstream_command: bin/fm-x-dismiss.sh
    upstream_flags: ["<target>"]
    contract: ts/src/tools.ts#relay_dismiss (approval + external relay consent)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:relay_dismiss"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate on top of external relay consent,
        which is never duplicated inside MCP; upstream fm-x-dismiss.sh knows no
        MCP approval string. Inert without that consent.
  - id: relay_followup
    kind: upstream-mirror
    summary: Send one public followup; inert without relay consent, which lives outside MCP.
    upstream_command: bin/fm-x-followup.sh
    upstream_flags: ["<target>", "<text>"]
    contract: ts/src/tools.ts#relay_followup (approval + external relay consent)
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:relay_followup"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate on top of external relay consent,
        which is never duplicated inside MCP; upstream fm-x-followup.sh knows
        no MCP approval string. Inert without that consent.
  - id: secondmate_nudge
    kind: upstream-mirror
    summary: Ask mismatched secondmates to reconcile through the cooldown-guarded notify path.
    upstream_command: bin/fm-secondmate-reconcile.sh
    upstream_flags: ["notify"]
    contract: schema/contracts.yaml#secondmate_nudge
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:secondmate_nudge"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the notify-only subset;
        upstream fm-secondmate-reconcile.sh knows no MCP approval string.
        request snapshot plumbing and watcher-internal process-requests stay
        out. Same owning script, narrower surface.
  - id: secondmate_restart
    kind: upstream-mirror
    summary: Restart secondmates onto current wiring after persist; ids only, no flags.
    upstream_command: bin/fm-secondmate-restart.sh
    upstream_flags: ["<secondmate-id>..."]
    contract: schema/contracts.yaml#secondmate_restart
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:secondmate_restart"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus 1..8 id validation;
        upstream fm-secondmate-restart.sh knows no MCP approval string. The
        persist gate and partial exit-3 vocabulary stay in the owning
        script. Same owning script, narrower surface.
  - id: secondmate_report
    kind: upstream-mirror
    summary: Append one correlated report to the parent channel; the helper resolves the destination.
    upstream_command: bin/fm-secondmate-report.sh
    upstream_flags: ["<verb>", "<corr>", "<note>"]
    contract: schema/contracts.yaml#secondmate_report
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:secondmate_report"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the note-only form with
        verb-slug and 16-hex corr validation; upstream
        fm-secondmate-report.sh knows no MCP approval string. The helper
        resolves the parent channel itself, so no status path crosses the
        boundary; --doc stays out.
  - id: remote_control
    kind: upstream-mirror
    summary: Closed state/route/observe/send subset of remote secondmate lifecycle control.
    upstream_command: bin/fm-remote-secondmate-control.sh
    upstream_flags: ["<verb>", "<id>"]
    contract: schema/contracts.yaml#remote_control
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:remote_control"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the closed
        state/route/observe/send verb set with send-text steer validation;
        upstream fm-remote-secondmate-control.sh knows no MCP approval
        string. launch/relaunch (firstmate-owned provisioning), key/capture
        (raw pane access), and sync/update/retire (remote code and pane
        teardown) stay out.
  - id: handoff_move
    kind: upstream-mirror
    summary: Hand queued backlog items to a secondmate, or resume pending wakes.
    upstream_command: bin/fm-backlog-handoff.sh
    upstream_flags: ["<secondmate-id>", "<item-key>..."]
    contract: schema/contracts.yaml#handoff_move
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:handoff_move"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus the move/resume-pending
        subset with 1..20 key validation; upstream fm-backlog-handoff.sh
        knows no MCP approval string. Moves only Queued items through the
        backend owner, which stays authoritative.
  - id: voice_queue
    kind: upstream-mirror
    summary: Hand one request to firstmate through the voice handover queue.
    upstream_command: bin/fm_voice_records.py
    upstream_flags: ["queue", "<text>"]
    contract: schema/contracts.yaml#voice_queue
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:voice_queue"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus single-line 1..500
        text validation; upstream fm_voice_records.py knows no MCP
        approval string. Handover queue only — no microphone, no audio,
        no Bedrock session; the mic client and relay stay out.
  - id: mail_send
    kind: upstream-mirror
    summary: Send one SMTP message; credentials live outside MCP.
    upstream_command: bin/fm-mail.sh
    upstream_flags: ["send", "<to>", "<subject>", "-"]
    contract: schema/contracts.yaml#mail_send
    upstream_test: "ts/tests/server.test.ts (stub-home server proof) + tests/fm-mcp-authz.test.sh (approval gate); no output-equivalence replay — writes never dispatch in conformance."
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:mail_send"]}
    divergence:
      status: intentional
      reason: >-
        Per-call "I authorize" approval gate plus validated to/subject
        with the body piped via stdin (never argv, never logged);
        upstream fm-mail.sh knows no MCP approval string. SMTP
        credentials live outside MCP in the home .env; poll/check-arm
        stay firstmate-owned.
  # ---- NEW: local-only Trillium extensions (survive upstream reconciliation) ----
  - id: drift_detection
    kind: local
    summary: Observed-surface drift detection seeded from the live fleet; diffs feature drift from behavior drift.
    local_path: drift/
    contract: drift/check.py (snapshot-compare CLI over drift/baseline.json)
    upstream_test: "n/a (local) — proof: tests/drift-check.test.sh + tests/test_drift.py"
    readme_anchor: Drift detection
    py: {status: implemented, evidence: ["drift/check.py:diff_snapshots", "drift/snapshot.py:def ", "drift/baseline.json:\"surfaces\""]}
    ts: {status: retired}
    divergence: {status: not-applicable, reason: ""}
  - id: upstream_shift_signal
    kind: local
    summary: Diffs the sources/firstmate pin against upstream main and reports which depended-on surfaces moved.
    local_path: drift/shift.py
    contract: drift/shift.py (pin-vs-main report feeding TS/Python port starts)
    upstream_test: "n/a (local) — proof: drift/shift.py CLI itself, documented in README Drift section (no dedicated suite)"
    readme_anchor: Upstream-shift signal
    py: {status: implemented, evidence: ["drift/shift.py:def build_report"]}
    ts: {status: retired}
    divergence: {status: not-applicable, reason: ""}
  - id: subprocess_envelope
    kind: local
    summary: Every tool call returns within 30s with process-group kill on timeout so timed-out reads leave no orphans; longer work detaches via async receipts.
    local_path: ts/src/constants.ts
    contract: ts/src/constants.ts (SUBPROCESS_TIMEOUT_S=30, MAX_OUTPUT_BYTES=1MB, RECEIPT_TIMEOUT_S=180, RECEIPT_TTL_S=3600; runner in ts/src/runner.ts)
    upstream_test: "n/a (local) — proof: ts/tests/server.test.ts fail-closed fixture (30s timeout kill, receipt lifecycle, cross-home isolation, expiry) + ts/tests/server.test.ts mirror"
    readme_anchor: Subprocess envelope
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/constants.ts:SUBPROCESS_TIMEOUT_S", "ts/src/runner.ts:MAX_OUTPUT_BYTES"]}
    divergence: {status: not-applicable, reason: ""}
  - id: receipt_submit
    kind: local
    summary: Detaches one tool call past the 30s fail-closed budget and returns a pending receipt with a receipt_status check signature; authority targets still need their nested approval.
    local_path: ts/src/constants.ts
    contract: schema/contracts.yaml#receipt_submit
    upstream_test: "n/a (local) — proof: ts/tests/server.test.ts receipt lifecycle (pending submit, done retrieval, cross-home isolation) + ts/tests/server.test.ts mirror"
    readme_anchor: Async receipts
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:toolReceiptSubmit", "ts/src/auth.ts:receipt_submit"]}
    divergence: {status: not-applicable, reason: ""}
  - id: receipt_status
    kind: local
    summary: Read-only check on one detached receipt; reports running, done with the result attached, failed, or expired past the TTL, confined to the serving home.
    local_path: ts/src/constants.ts
    contract: schema/contracts.yaml#receipt_status
    upstream_test: "n/a (local) — proof: ts/tests/server.test.ts receipt lifecycle (running/done/failed, expiry, cross-home isolation) + ts/tests/server.test.ts mirror"
    readme_anchor: Async receipts
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:toolReceiptStatus", "ts/src/auth.ts:receipt_status"]}
    divergence: {status: not-applicable, reason: ""}
  - id: auth_tiers
    kind: local
    summary: Every tool carries a code-assigned tier; the audit log records authority use; authority tools refuse without an authorization string.
    local_path: ts/src/auth.ts
    contract: ts/src/auth.ts (TOOL_TIERS + approval strings + duration_ms timing)
    upstream_test: "n/a (local) — proof: tests/fm-mcp-authz.test.sh + ts/tests/auth.test.ts + ts/tests/auth.test.ts"
    readme_anchor: Auth tiers in code
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/auth.ts:APPROVAL_PREFIX"]}
    divergence: {status: not-applicable, reason: ""}
  - id: contract_map
    kind: local
    summary: Depended-on subset declaration with stability tiers; the validator fails naming the stale pin.
    local_path: schema/
    contract: schema/contracts.yaml (declared subset) + schema/contracts.schema.json (shape)
    upstream_test: "n/a (local) — proof: tests/mcp-schema.test.sh over schema/validate.py"
    readme_anchor: Contract map
    py: {status: implemented, evidence: ["schema/validate.py:CURRENT_PINS", "schema/contracts.yaml:name:"]}
    ts: {status: retired}
    divergence: {status: not-applicable, reason: ""}
  - id: conformance_fixtures
    kind: local
    summary: Stub-home fixtures proving adapter output equals the owning scripts' output for both implementations.
    local_path: tests/conformance/
    contract: tests/conformance/README.md (conformance referee for TypeScript server)
    upstream_test: "n/a (local) — proof: tests/conformance/conformance.sh + tests/conformance/ts-parity.sh (py/ts payloads field-for-field)"
    readme_anchor: Conformance fixtures
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/tests/conformance.test.ts:test", "ts/src/server.ts:handleToolsCall"]}
    divergence: {status: not-applicable, reason: ""}
  - id: typescript_sibling
    kind: local
    summary: Independent TypeScript implementation of the same tool contract over stdio with no runtime dependencies.
    local_path: ts/
    contract: ts/src/tools.ts (tool registry) + ts/src/server.ts (stdio server)
    upstream_test: "n/a (local) — proof: ts/tests/server.test.ts (71-check suite) via bun run test:bun and npm test"
    readme_anchor: TypeScript sibling
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/server.ts:server", "ts/src/tools.ts:fleet_snapshot"]}
    divergence: {status: not-applicable, reason: ""}
  - id: followon_actions
    kind: local
    summary: Generalized customizable follow-on actions and action chains for MCP tool executions with condition evaluation, context forwarding, auth tier enforcement, and loop prevention.
    local_path: ts/src/followon.ts
    contract: docs/FOLLOWON_DESIGN.md (trigger model, condition predicates, context forwarding, anti-laundering auth gates, and loop termination proof)
    upstream_test: "n/a (local) — proof: ts/tests/followon.test.ts (24-check suite) via bun run test:bun and npm test"
    readme_anchor: Customizable follow-on actions
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/followon.ts:FollowOnService", "ts/src/followon.ts:executeFollowOnsEffect", "ts/tests/followon.test.ts:describe"]}
    divergence: {status: not-applicable, reason: ""}
  # ---- FORK DELTAS: trillium/firstmate behavior changes vs upstream kunchenguid ----
  - id: brief_herdr_lab_safety
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-brief.sh
    summary: Hard safety lifecycle declaration gate in brief generation (--herdr-lab) preventing unprompted Herdr lifecycle commands.
    contract: schema/contracts.yaml#scaffold_brief
    upstream_test: "proof: tests/upstream/run_upstream.py::scaffold_brief (fm-brief.test.sh: --herdr-lab safety contract)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:scaffold_brief"]}
    divergence:
      status: intentional
      reason: >-
        Trillium fork requires explicit --herdr-lab and strict lifecycle safety
        gate injection for agent briefs; upstream brief generation has no
        Herdr lab contract.
  - id: spawn_batch_delivery
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-spawn.sh
    summary: Batch crew dispatch with shared delivery contract verification across id=repo pairs and project-scoping.
    contract: schema/contracts.yaml#spawn_crew
    upstream_test: "proof: tests/upstream/run_upstream.py::spawn_crew (fm-spawn-batch.test.sh: batch dispatch & delivery contract)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:spawn_crew"]}
    divergence:
      status: intentional
      reason: >-
        Trillium fork adds batch dispatching, project path scoping through
        firstmate home, and delivery contract validation.
  - id: remote_fleet_management
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-remote-doctor.sh
    summary: Persistent secondmate control and remote home coordination across Tailscale minis with state reconciliation.
    contract: schema/contracts.yaml#remote_doctor
    upstream_test: "proof: ts/tests/conformance.test.ts (scratch FM_HOME remote and secondmate suites)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:remote_doctor"]}
    divergence:
      status: intentional
      reason: >-
        Multi-machine fleet management and secondmate persistence across remote
        nodes specific to Trillium's infrastructure.
  - id: backlog_handoff_protocol
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-backlog-handoff.sh
    summary: Structured task handoff outboxes (data/handoff/<id>.outbox.md) and atomic move protocol between crews.
    contract: schema/contracts.yaml#handoff_status
    upstream_test: "proof: ts/tests/conformance.test.ts (handoff status & move tests)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:handoff_status"]}
    divergence:
      status: intentional
      reason: >-
        Asynchronous crew-to-crew handoff protocol via structured markdown
        outboxes.
  - id: captain_decision_holds
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-decision-hold.sh
    summary: Structured captain hold buckets, age-based triage, and closed-verdict recording (fm-decision-hold.sh, fm-captain-hold.sh).
    contract: schema/contracts.yaml#review_decision
    upstream_test: "proof: ts/tests/server.test.ts (decision hold & review decision verification)"
    py: {status: retired}
    ts: {status: implemented, evidence: ["ts/src/tools.ts:review_decision"]}
    divergence:
      status: intentional
      reason: >-
        Structured decision hold lifecycle with answer files and explicit
        verdict transitions.
  - id: fork_origin_check
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-fork-origin-check.sh
    summary: Advisory scan for registered project clones whose remotes look like an unswapped fork-contribution setup.
    contract: drift/baseline.json
    upstream_test: "proof: drift/baseline.json:\"fm-fork-origin-check.sh\""
    py: {status: implemented, evidence: ["drift/baseline.json:\"fm-fork-origin-check.sh\""]}
    ts: {status: missing}
    divergence:
      status: intentional
      reason: >-
        Trillium fork-origin convention for fork-contribution clones; upstream
        firstmate does not enforce or scan fork remote setup.
  - id: afk_daemon_launch
    kind: fork
    fork_rev: 86c035336bf9e136dd44d76e7d46e16d260fac8f
    fork_command: bin/fm-afk-launch.sh
    summary: Away-mode daemon terminal lifecycle in non-visible tracked terminal with supervisor targeting and clean persona isolation without PAI config bleed.
    contract: drift/baseline.json
    upstream_test: "proof: drift/baseline.json:\"fm-afk-launch.sh\""
    py: {status: implemented, evidence: ["drift/baseline.json:\"fm-afk-launch.sh\""]}
    ts: {status: missing}
    divergence:
      status: intentional
      reason: >-
        Non-visible terminal creation and PAI isolation fix; upstream uses
        foreground or visible pane splits.
