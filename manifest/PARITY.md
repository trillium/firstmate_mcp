# Kun parity report

Generated by `scripts/gen_parity.py` from `manifest/COVERAGE.md`, `manifest/FEATURES.yaml` and the contract index. Bead: `project-2od.1` (standing order — upstream current matters more than our additions; ours are pinned and reported, not reimplemented).

## Surface parity: every current upstream command is classified

- upstream command surfaces enumerated: **158**
- mirrored: **65**
- denied-by-design: **70** (deliberate refusals, each with a reason in COVERAGE.md)
- gap rows: **23** — of which removed-upstream **23**, current-upstream **0**

**No buildable gap remains**: every command the pinned upstream still ships is either mirrored through a tool or refused by design. The gap rows are commands upstream itself removed since the pin; they are history, not backlog.

## Capability parity is deliberately partial

Mirror parity is not capability parity: the denied-by-design set is the point of the doorway, not a shortfall. Denied surfaces by area (reason for each in COVERAGE.md):

- Supervision: 23
- Sessions: 17
- Secondmates / remotes: 10
- Installs: 10
- PR pipeline: 5
- Fleet runs: 2
- Relay: 2
- Backlog / decisions: 1

## Our changes: pinned and reported, not implemented

- features in `manifest/FEATURES.yaml`: **107**
  - kind `upstream-mirror`: 85
  - kind `local`: 15
  - kind `fork`: 7
- TypeScript status: implemented 102, missing 2, retired 3
- divergence status: intentional 89, none 3, not-applicable 15

## Served-line parity (the dimension surface parity does not cover)

Run with `--fm-home PATH` to report which declared contracts that home can run.
