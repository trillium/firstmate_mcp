# Kun parity report

Generated by `scripts/gen_parity.py` from `manifest/COVERAGE.md`, `manifest/FEATURES.yaml` and the contract index. Bead: `project-2od.1` (standing order — upstream current matters more than our additions; ours are pinned and reported, not reimplemented).

## Surface parity: every current upstream command is classified

- upstream command surfaces enumerated: **160**
- mirrored: **67**
- denied-by-design: **72** (deliberate refusals, each with a reason in COVERAGE.md)
- gap rows: **0** — of which removed-upstream **0**, current-upstream **0**
- fork extensions: **21** (trillium/firstmate additions, spine-classified, not upstream gaps)

**No buildable gap remains**: every command the pinned upstream still ships is either mirrored through a tool or refused by design. The gap rows are commands upstream itself removed since the pin; they are history, not backlog.

## Capability parity is deliberately partial

Mirror parity is not capability parity: the denied-by-design set is the point of the doorway, not a shortfall. Denied surfaces by area (reason for each in COVERAGE.md):

- Supervision: 23
- Sessions: 18
- Secondmates / remotes: 10
- Installs: 10
- PR pipeline: 5
- Fleet runs: 2
- Backlog / decisions: 2
- Relay: 2

## Our changes: pinned and reported, not implemented

- features in `manifest/FEATURES.yaml`: **125**
  - kind `upstream-mirror`: 87
  - kind `fork`: 20
  - kind `local`: 18
- TypeScript status: implemented 112, missing 10, retired 3
- divergence status: intentional 103, none 4, not-applicable 18

## The fork line: proof basis vs the line the fleet runs

- proof basis (`proven_commit`): `86c035336bf9e136dd44d76e7d46e16d260fac8f` (2026-09-17)
- served line (`served_commit`): `aaf67489ec7644f3bdfd4043ba0c898ab928bcd9` (2026-09-06)

They are different revs: the proof basis is when CUTOVER-PROOF.md and UPSTREAM-RESULTS.md were produced; the served line is what `FM_HOME` actually runs. `manifest/validate.py` fails loudly if the served line and `drift/baseline.json` disagree, and `schema/validate.py` fails if the two files state different served revs. Keeping the fleet on a current line is operational (`project-2od.13`), not a doorway concern.

## Served-line parity (the dimension surface parity does not cover)

Run with `--fm-home PATH` to report which declared contracts that home can run.
