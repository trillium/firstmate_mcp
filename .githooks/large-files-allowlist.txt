# Large file allowlist — one glob pattern per line.
# Files matching these patterns are exempt from the 250-line pre-commit warning.
#
# Seeded 2026-09-23 with 12 legacy violators; 10 decomposed under task-8pqjb
# (slices 1-30) and removed. The 2 below remain with dated reasons; anything
# NEW over 250 lines warns.
# 2026-09-23 (task-8pqjb slice 19): ts/src/followon.ts engine core (~630
# lines) cannot split further without breaking up executeFollowOnsEffect, one
# cohesive Effect program, or moving module-eval layer constructors
# (FollowOnLive) across a TDZ-unsafe import cycle. Revisit only with a
# behavior-preserving engine refactor + full receipt/envelope proof.
# 2026-09-23 (task-8pqjb slice 22): ts/src/http.ts remainder (~400 lines) is
# one giant closure-bodied createHttpServer + thin foundation. Splitting route
# sections means threading ~8 shared locals through extracted closures —
# behavior risk with no behavior gain. Revisit only with per-route
# request/response contract tests first.
