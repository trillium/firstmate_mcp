{
  "name": "firstmate-mcp-ts",
  "version": "0.3.0",
  "description": "TypeScript sibling of the First Mate MCP smarts-only server (stdio parity, Effect composition)",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "firstmate-mcp-ts": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:tests": "tsc -p tsconfig.tests.json",
    "clean": "rm -rf dist testbuild",
    "test": "npm run build && npm run build:tests && node --test testbuild/tests/*.test.js",
    "test:bun": "npm run build && npm run build:tests && bun test testbuild/tests/*.test.js",
    "test:fast": "npm run build && npm run build:tests && node --test testbuild/tests/validators.test.js testbuild/tests/envelope.test.js testbuild/tests/auth.test.js",
    "test:fast:bun": "npm run build && npm run build:tests && bun test testbuild/tests/validators.test.js testbuild/tests/envelope.test.js testbuild/tests/auth.test.js",
    "conformance": "npm run build && npm run build:tests && node --test testbuild/tests/conformance.test.js",
    "conformance:bun": "npm run build && npm run build:tests && bun test testbuild/tests/conformance.test.js",
    "prove": "npm run build && npm run build:tests && node --test testbuild/tests/*.test.js",
    "prove:bun": "npm run build && npm run build:tests && bun test testbuild/tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4"
  },
  "dependencies": {
    "effect": "3.22.2"
  }
}
